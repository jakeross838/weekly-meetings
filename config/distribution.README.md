# Distribution Config

`pm_emails` maps each Project Manager to their email for the weekly meeting attachment.

`always_cc` lists addresses to CC on every email regardless of PM:
- `lee@rossbuilt.com` — **Lee Ross**, owner; CC'd on every PM's weekly so he sees all five operations
- `andrew@rossbuilt.com` — **Andrew**, ops oversight
- `jake@rossbuilt.com` — **Jake**, system author

Note: `lee@rossbuilt.com` (always_cc) is a DIFFERENT person from `lworthy@rossbuilt.com` (Lee Worthy PM mapping). Do not consolidate.
